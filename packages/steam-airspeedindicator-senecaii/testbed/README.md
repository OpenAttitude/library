# Instrument testbed

Same pattern as `@openattitude/steam-attitudegyro-senecaii`: `npm run dev:testbed` (port 5175).

See that package’s `testbed/README.md` for the repeatable checklist when adding new instruments.
